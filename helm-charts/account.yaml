replicaCount: 1

app:
  name: my-name-cs-si11

Release:
  Maintainer: my-service
  name: service

env:
  - name: MY_ID
    value: one
  - name: ONE_ID
    value: two

containerPort: 8080

image:
  repository: my-repo/my-image
  tag: latest

resources:
  limits:
    cpu: "500m"
    memory: "512Mi"
  requests:
    cpu: "250m"
    memory: "256Mi"
